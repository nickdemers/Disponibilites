<%= form_for(@remplacement) do |f| %>
  <% if @remplacement.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@remplacement.errors.count, "error") %> prohibited this remplacement from being saved:</h2>

      <ul>
      <% @remplacement.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :id_utilisateur %><br />
    <%= f.number_field :id_utilisateur %>
  </div>
  <div class="field">
    <%= f.label :id_event_calendar %><br />
    <%= f.text_field :id_event_calendar %>
  </div>
  <div class="field">
    <%= f.label :id_utilisateur_remplacant %><br />
    <%= f.number_field :id_utilisateur_remplacant %>
  </div>
  <div class="field">
    <%= f.label :statut %><br />
    <%= f.text_field :statut %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
